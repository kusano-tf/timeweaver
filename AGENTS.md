# Timeweaver Agent Guide

Timeweaver を実装・変更するコーディングエージェントは、作業前に `.agents/implementation-guide.md` を確認する。

特に Git のコミットメッセージは日本語で書く。

JSON 形式は、明示的な指示がない限り開発中の `schemaVersion: "1.0.0"` として扱う。仕様変更だけを理由にスキーマバージョンを上げず、互換移行も追加しない。
